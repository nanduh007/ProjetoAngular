import { Component } from '@angular/core';

@Component({
    // selector: 'app-home', // não precisa mais do selector pois o está sendo linkado por rota e não pelo selector
    templateUrl: './app-home.component.html'
})
export class HomeComponent{

}